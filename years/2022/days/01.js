const partOne = (input) => {
  return 0;
};

const partTwo = (input) => {
  return 0;
};

module.exports = {
  // beforeAll, // optional - function to run before the solutions are run
  // beforeEach, // optional - function to run before each solution
  // parseInput, // options - defaults to splitting input into an array of lines
  partOne, // required - function that takes in input and returns the answer for partOne
  partTwo, // required - function that takes in input and returns the answer for partTwo
  testing: false, // optional - if set to true use 01.test.txt instead of 01.txt
  runPartOne: true, // optional - if set to false partOne will not be executed
  runPartTwo: true, // optional - if set to false partTwo will not be executed
};
